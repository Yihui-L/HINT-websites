# Example only; do not run concurrently with the initial writer.
# Current schema 21 restores B0/B/p/v/s and source/wall data from the same file.
[paths]
output_file = "run/hint_debug_ncsx_vmec.nc"
[solver]
mode = "follow"
backend = "gpu"
engine = "jax"
outer_steps = 50
save_every = 1
# Omitted solver controls inherit the checkpoint, including interpolation and scale_after=false.
[output]
compression_level = 4
store_stepb_diagnostics = true
